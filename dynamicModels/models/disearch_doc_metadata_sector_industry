'use strict';
const { Model } = require('sequelize');

module.exports = (sequelize, DataTypes, schema) => {
    class DisearchDocMetadataSectorIndustry extends Model {
        static associate(models) {
            // Define associations here
        }
    }

    DisearchDocMetadataSectorIndustry.init(
        {
            file_id: {
                type: DataTypes.STRING,
                allowNull: false
            },
            value: DataTypes.STRING(100000),
        },
        {
            sequelize,
            modelName: `${schema}_disearch_doc_metadata_sector_industry`,
            tableName: 'disearch_doc_metadata_sector_industry',
            schema,
            createdAt: false,
            updatedAt: false
        }
    );

    return DisearchDocMetadataSectorIndustry;
};
